#!/bin/bash

if [[ "$1" == "-y" ]] || gum confirm "Refresh Plymouth theme? This will replace your current startup screen with ProjTC defaults."; then
  sudo cp ~/.local/share/projtc/default/plymouth/* /usr/share/plymouth/themes/projtc/
  sudo plymouth-set-default-theme -R projtc
fi
